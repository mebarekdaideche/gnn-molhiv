# configs/gcn_config.yaml
# Hyperparamètres pour GCN — OGB-MolHIV
# Usage : python -m src.train --config configs/gcn_config.yaml

model: gcn
num_layers: 5
emb_dim: 300
dropout: 0.5
epochs: 100
lr: 0.001
weight_decay: 0.0
batch_size: 32
seed: 42
data_root: dataset
